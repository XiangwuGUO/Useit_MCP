# MCP Core Dependencies
-e ../../../python-sdk
mcp>=1.0.0
fastmcp>=0.1.0

# Data Validation
pydantic>=2.0.0

# LLM Integration
openai>=1.0.0

# Image Processing
Pillow>=10.0.0

# Windows Automation & Screenshot Capture
pywin32>=305  # For Excel COM automation and window capture
pyautogui>=0.9.54  # Fallback for screenshot capture

# Notes:
# 1. PowerShell is built into Windows, no additional installation needed
# 2. pywin32 is now REQUIRED for Excel COM operations and screenshot capture
# 3. pyautogui is optional fallback if pywin32 screenshot fails
# 4. Ensure Microsoft Excel is installed on the system
# 5. API keys required: OpenAI API key for LLM calls (stage 1 & 2)
