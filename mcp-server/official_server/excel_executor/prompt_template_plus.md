这是**第 N 轮**修改。
请在**默认沿用第一轮全部规则**的前提下继续工作，包括但不限于：

* J 必须在 [-0.5, 2]
* K 只能在四档之间切换
* 动态 Offset 必须持续追踪
* “接消力池”必须红底
* “跌水2.2”必须橙底
* 修改比降必须在超标行的上游进行
* 一段只改一次的策略
* 输入步骤必须是（Offset=...）格式，输出按“两部分”结构

---

## 本轮任务

1. 视为**上一轮的全部步骤已被执行完**，Excel 已重新计算 J 列。
2. 我会给你本轮的 Excel 最新截图 / J 列最新数值。
3. 你需要继续扫描并找出仍然：

[
J < -0.5 \quad 或 \quad J > 2
]

的所有区段。

---

## 输出要求（仅输出本轮新增操作）

请继续使用上一轮的编号或从步骤 1 重编号皆可，但必须包含：

### **部分一：本轮新增的规划说明**

只描述本轮新发现的问题区段，并给出新增的控制行 / 新的跌水位置，避免重复前几轮已经修过但现在不需要动的部分。

### **部分二：本轮新增的 Excel 步骤**

必须包含：

* `(Offset = 当前偏移量)`
* 修改比降 → 写“接消力池”并标红
* 添加跌水 → 写“跌水2.2”并标橙
* 插入新行 → 更新 Offset
* 所有行号都按“原始行号 + 当前 Offset”计算

---

## 完成条件

如果你判断：
“按本轮操作执行后，理论上所有 J 均可落入 [-0.5, 2]”，

请在规划最后写一句：

> **如执行上述步骤并重新检查 J 列后仍有超标，请提供新的截图以进入下一轮迭代。**
