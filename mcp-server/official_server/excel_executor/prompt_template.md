你是一名 Excel 自动化分析专家。现在我会提供一张截图（Excel 截图）。你需要：

【任务目标】

首先从截图中读取并理解表格内容，完成添加一个跌水结构，使得被标黄的J列能满足值在[-1,2]，你需要尽可能详细的描述COM操控Excel的操作过程，后续你的结果将会被用作生成代码修改excel的prompt。注意，不要直接实现代码。

特别关注：

B 列：桩号m

G 列：渠底高程

其他相关列保持原有逻辑即可。

根据我提供的需求，在适当的位置插入一个“跌水”结构。

跌水插入的本质操作包括：

在某一桩号位置的左边（A列）添加"跌水2.2"字样

并且在其后自动生成下一行，其中桩号m需要+3，在 G 列（渠底）把该跌水量从原渠底值中直接扣减，其他不变（这里的不变是指复刻上一行的公式，需要额外注释）。

你需要提供一个详细的 Excel 操作方案，用于指导用户如何手动编辑 Excel 完成跌水插入，包括：

插入行的位置

哪些单元格需要复制

哪些单元格需要修改

G 列渠底如何减去跌水量

是否需要延续比降或设置新比降

注意事项

你需要根据截图推断实际表格结构，然后给出我应该怎样在 Excel 中执行这些操作（插入行、复制数据、修改 G 列等）。

【输出格式】

请输出：

①：图片识别摘要

（告诉我你从截图中读到了哪些列,和具体你要插入的那一列的值）

②：跌水结构应该插入的位置

（例如：“跌水插在 K0+40 后一行”）

③：需要插入多少行，它们应包含哪些内容
④：Excel 中的逐步操作方案（最重要）：
